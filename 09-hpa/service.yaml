apiVersion: v1
kind: Service
metadata:
  name: workshop-hpa-svc
  namespace: workshop
  labels:
    app: workshop-hpa-app
spec:
  type: ClusterIP
  selector:
    app: workshop-hpa-app
  ports:
  - name: http
    port: 80
    targetPort: 80
